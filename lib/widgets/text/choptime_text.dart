import 'package:flutter/material.dart';

class ChowTimeText extends StatelessWidget {
  const ChowTimeText({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
